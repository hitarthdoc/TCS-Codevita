#include "stdio.h"

int int main(int argc, char const *argv[])
{
    /* code */
    int n, x[1000], y[1000], t[1000];
    double *p;

    scanf("%d", &n);

    for (int i = 0; i < n; i++)
    {
        scanf("%d %d %d", &x[i], &y[i], &t[i]);
    }

    
    return 0;
}
